# Supabase Configuration
# These variables are PUBLIC and will be exposed to the client-side code
# The PUBLIC_ prefix makes them available in browser JavaScript

# Supabase Project URL
PUBLIC_SUPABASE_URL=http://127.0.0.1:54321

# Supabase Anonymous Key (safe to expose)
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Note: For local development with Supabase CLI, use:
# npx supabase start
# This will output the local credentials that you should use above
